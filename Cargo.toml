[package]
name = "json-gettext"
version = "3.1.0"
authors = ["Magic Len <len@magiclen.org>"]
repository = "https://github.com/magiclen/json-gettext"
homepage = "https://magiclen.org/json-gettext"
keywords = ["json", "i18n", "multi-language", "static", "rocket"]
categories = ["internationalization"]
description= "A library for getting text from JSON usually for internationalization."
readme = "README.md"
license = "MIT"

[badges]
travis-ci = { repository = "magiclen/json-gettext", branch = "master" }
appveyor = { repository = "magiclen/json-gettext", branch = "master", service = "github" }

[dependencies]
serde = "1.0.72"
serde_json = "1.0.33"
regex = "1"
rocket = {version = "0.4", optional = true}

[dev-dependencies]
rocket = "0.4"

[features]
rocketly = ["rocket"]